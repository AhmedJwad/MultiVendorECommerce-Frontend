import { Box, Typography } from "@mui/material"

const StoreList=()=>{
    return (
        <Box sx={{p:3}}>
            <Typography variant="h4" gutterBottom>
                welcome to StoreList
            </Typography>            
        </Box>
    )
}

export default StoreList;